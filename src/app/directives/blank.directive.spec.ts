import { BlankDirective } from './blank.directive';

describe('BlankDirective', () => {
  it('should create an instance', () => {
    const directive = new BlankDirective();
    expect(directive).toBeTruthy();
  });
});
